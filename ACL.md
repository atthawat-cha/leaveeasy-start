# ACL.md — สิทธิ์การเข้าถึงข้อมูลของ LeaveEasy

> เอกสารนี้สรุปสิทธิ์ (Access Control) ของผู้ใช้แต่ละบทบาท อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท) และ CLAUDE.md หัวข้อ 3 (สถานะใบลา)
>
> ⚠️ **สถานะการบังคับใช้จริง (ตามที่เขียนเอกสารนี้):** Firestore Security Rules ปัจจุบันบังคับแค่กฎขั้นต่ำ "ต้องล็อกอินก่อน ถึงอ่าน/เขียนได้" (`firestore.rules`) — **ยังไม่มีการเช็คแยกตามบทบาทหรือความเป็นเจ้าของใบลาจริงในกฎ** เอกสารนี้คือพิมพ์เขียวสิทธิ์ที่ต้องมีในสัปดาห์ที่ 8 (กฎรายห้องครบชุด)

---

## 1. บทบาทผู้ใช้

| บทบาท | ค่า `role` ที่เก็บใน Firestore | ชื่อเรียกบนหน้าจอ |
|---|---|---|
| 🙋 ผู้ขอลา | `employee` | ผู้ขอลา |
| ✅ ผู้อนุมัติ | `manager` | ผู้อนุมัติ |
| 🧾 ฝ่ายบุคคล | `hr` | ฝ่ายบุคคล |

ระบุใน `users/{uid}` ฟิลด์ `role` เท่านั้น มีแค่ 3 ค่านี้ ไม่มีบทบาทที่ 4

---

## 2. ตารางสิทธิ์ตามทรัพยากร (Resource) × บทบาท

### 2.1 `leaveRequests` (ใบขอลา)

| การกระทำ | 🙋 employee | ✅ manager | 🧾 hr |
|---|---|---|---|
| สร้างใบลาใหม่ (เป็นของตัวเอง) | ✅ | ✅ | ✅ |
| ดูใบลาของ**ตัวเอง** (`requesterId == uid` ของตน) | ✅ | ✅ | ✅ |
| ดูใบลาของ**คนอื่น** | ❌ | ✅ (ทุกใบ) | ✅ (ทุกใบ) |
| แก้ไขฟิลด์ `status` (อนุมัติ/ไม่อนุมัติ) | ❌ | ✅ (ทุกใบ) | ✅ (ทุกใบ) |
| กำหนด/แก้ `approverId`, `approverName` | ❌ | ❌ | ✅ |
| ลบใบลา | ✅ เฉพาะใบของตัวเอง **และ** สถานะยังเป็น `รอพิจารณา` | ❌ (ลบใบของคนอื่นไม่ได้) | ❌ (ลบใบของคนอื่นไม่ได้) |

> 📌 **employee ไม่มีสิทธิ์เปลี่ยนสถานะใบลาเลย ไม่ว่าใบของตัวเองหรือของคนอื่น** — การอนุมัติ/ไม่อนุมัติเป็นสิทธิ์ของ `manager`/`hr` เท่านั้น จึงไม่มีทางที่ employee จะ "อนุมัติใบของตัวเอง" ได้ตั้งแต่ต้น
>
> 📌 **employee เปิดดูใบลาของผู้ขอลาคนอื่นไม่ได้** แม้จะเป็นใบที่ยัง `รอพิจารณา` ก็ตาม — เห็นเฉพาะใบที่ `requesterId` ตรงกับ uid ของตัวเองเท่านั้น
>
> 📌 **manager และ hr ดูใบลาได้ทุกใบและอนุมัติได้ทุกใบ** ไม่จำกัดว่าใครเป็นผู้ขอลา (รวมถึงใบของ manager/hr คนอื่นด้วย — ระบบนี้ยังไม่มีกฎ "ห้ามอนุมัติใบของตัวเอง" สำหรับ manager/hr เพราะสเปกไม่ได้ระบุไว้)

### 2.2 `leaveRequests/{id}/approvals` (ความเห็นการอนุมัติ)

| การกระทำ | 🙋 employee | ✅ manager | 🧾 hr |
|---|---|---|---|
| ดูความเห็นของใบที่ตัวเองมีสิทธิ์ดู | ✅ (เฉพาะใบของตัวเอง) | ✅ (ทุกใบ) | ✅ (ทุกใบ) |
| เขียนความเห็นในใบของ**ตัวเอง** | ✅ | ✅ | ✅ |
| เขียนความเห็นในใบของ**คนอื่น** | ❌ | ✅ (ทุกใบ) | ✅ (ทุกใบ) |
| ลบ/แก้ไขความเห็นเดิม | ❌ | ❌ | ❌ |

### 2.3 `leaveTypes` (ประเภทการลา)

| การกระทำ | 🙋 employee | ✅ manager | 🧾 hr |
|---|---|---|---|
| อ่านรายการ (ใช้เติมดรอปดาวน์ตอนยื่นใบลา) | ✅ | ✅ | ✅ |
| เพิ่ม / แก้ไข / ลบ ประเภทการลา | ❌ | ❌ | ✅ |

### 2.4 `users` (ผู้ใช้)

| การกระทำ | 🙋 employee | ✅ manager | 🧾 hr |
|---|---|---|---|
| อ่าน/แก้ไขข้อมูลของ**ตัวเอง** | ✅ | ✅ | ✅ |
| อ่านข้อมูลของคนอื่น (เพื่อแสดงชื่อผู้ขอลา/ผู้อนุมัติ) | ✅ (จำกัดเท่าที่ต้องแสดงชื่อ) | ✅ | ✅ |
| แก้ไขฟิลด์ `role` ของตัวเองหรือคนอื่น | ❌ | ❌ | ❌ (ระบบยังไม่มีหน้าจอจัดการ role) |

---

## 3. กติกาการเปลี่ยนสถานะใบลา (อ้างอิง CLAUDE.md หัวข้อ 3)

| สถานะ | ใครเปลี่ยนเป็นค่านี้ได้ | เปลี่ยนต่อไปเป็นอะไรได้ |
|---|---|---|
| `รอพิจารณา` | ระบบตั้งอัตโนมัติตอนสร้างใบใหม่ (ไม่มีบทบาทไหนเลือกเองได้) | `อนุมัติ` หรือ `ไม่อนุมัติ` |
| `อนุมัติ` | `manager` · `hr` เท่านั้น | ปลายทาง เปลี่ยนต่อไม่ได้ |
| `ไม่อนุมัติ` | `manager` · `hr` เท่านั้น (ต้องมีความเห็นใน `approvals` อย่างน้อย 1 รายการก่อน) | ปลายทาง เปลี่ยนต่อไม่ได้ |

- แก้สถานะทุกครั้งแก้เฉพาะฟิลด์ `status` ห้ามเขียนทับฟิลด์อื่นในไฟล์เดิม
- `employee` ไม่มีสิทธิ์เรียก action นี้เลย ไม่ว่าใบของใคร

---

## 4. สรุปสั้น (Quick Reference)

```
employee (ผู้ขอลา)
  ✅ ยื่นใบลาใหม่
  ✅ ดูใบลาของตัวเอง
  ✅ เขียนความเห็นในใบของตัวเอง
  ✅ ลบใบของตัวเองที่ยัง "รอพิจารณา"
  ❌ ดูใบลาของคนอื่น
  ❌ เปลี่ยนสถานะใบลา (จึงอนุมัติใบของตัวเองไม่ได้โดยปริยาย)
  ❌ แก้ไขประเภทการลา

manager (ผู้อนุมัติ)
  ✅ ดูใบลาทุกใบ
  ✅ อนุมัติ / ไม่อนุมัติ ทุกใบ
  ✅ เขียนความเห็นในทุกใบ
  ❌ ลบใบลาของคนอื่น
  ❌ แก้ไขประเภทการลา

hr (ฝ่ายบุคคล)
  ✅ ทำได้ทุกอย่างที่ manager ทำได้
  ✅ กำหนดผู้อนุมัติ (approverId/approverName) ให้แต่ละใบ
  ✅ เพิ่ม/แก้/ลบ ประเภทการลา
```

---

## 5. อ้างอิง

- โครงสร้างข้อมูลและกติกาสถานะฉบับเต็ม: `CLAUDE.md`
- User Story ต้นทางของสิทธิ์แต่ละบทบาท: `leaveeasy-spec.md` หัวข้อ 2 และ US-04, US-05, US-06, US-07, US-08
- กฎเฝ้าข้อมูลที่ deploy จริงอยู่ตอนนี้ (ขั้นต่ำ ยังไม่แยกบทบาท): `firestore.rules`
