# CLAUDE.md — LeaveEasy

> เอกสารนี้คือความจำถาวรของโปรเจกต์สำหรับ Claude Code อ่านรายละเอียดฉบับเต็มได้ที่ `leaveeasy-spec.md`

---

## 1. ระบบนี้ทำอะไร

**LeaveEasy** คือระบบขอลาออนไลน์ของที่ทำงาน แทนที่การขอลาด้วยกระดาษ/แชท/ปากเปล่า

**เส้นทางหลัก:** พนักงานยื่นใบลา → หัวหน้าพิจารณา → อนุมัติหรือไม่อนุมัติ → บันทึกผลให้ทุกฝ่ายเปิดดูย้อนหลังได้

กลุ่มเป้าหมาย: ที่ทำงานขนาด 20–100 คน มีหัวหน้าผู้อนุมัติ 1–3 คน

### ผู้ใช้ 3 บทบาท (`role`)

| ค่า `role` | บทบาท | ทำอะไรได้ |
|---|---|---|
| `employee` | ผู้ขอลา | ยื่นใบลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นในใบของตัวเอง · ลบใบของตัวเองที่ยังเป็น `รอพิจารณา` |
| `manager` | ผู้อนุมัติ | ดูใบลาทุกใบ · เปลี่ยนสถานะ (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ |
| `hr` | ฝ่ายบุคคล | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · จัดการประเภทการลา · ดูแดชบอร์ดสรุป |

### สแตกเทคนิค (บังคับ ห้ามเปลี่ยนเอง)

- HTML · CSS · JavaScript ธรรมดา — **ห้ามใช้ framework** (React, Vue, Next.js, Tailwind ฯลฯ)
- แยกไฟล์ `.html` ต่อหน้าจอ · CSS อยู่ใน `css/` · JS อยู่ใน `js/`
- **ไม่มีเซิร์ฟเวอร์ที่เขียนเอง** — หน้าเว็บคุยกับฐานข้อมูลตรง ๆ
- ฐานข้อมูล: **Firestore** · ล็อกอิน: **Firebase Authentication** · ยามเฝ้าข้อมูล: **Security Rules** · ขึ้นออนไลน์: **Firebase Hosting**

---

## 2. Collection ทั้งหมดใน Firestore

```
📁 users/            📄 { name, email, role }
📁 leaveTypes/        📄 { name }
📁 leaveRequests/     📄 { title, reason, status,
                            requesterId, requesterName,
                            approverId,  approverName,
                            leaveTypeId, leaveTypeName,
                            startDate, endDate, createdAt,
                            attachmentUrl }   ← เพิ่มสัปดาห์ที่ 9
      📁 approvals/   📄 { authorId, authorName, message, createdAt }
```

- **`users`** — ผู้ใช้ในระบบ ฟิลด์ `role` เก็บเป็นรหัสอังกฤษ (`employee`/`manager`/`hr`) เท่านั้น เพราะใช้เขียน Security Rules
- **`leaveTypes`** — ประเภทการลา จัดการได้เฉพาะ `hr`
- **`leaveRequests`** — ใบขอลา คือ collection หลักของระบบ
- **`approvals`** — subcollection ซ้อนอยู่ใต้ `leaveRequests/{id}/approvals` เก็บความเห็นการอนุมัติของใบนั้น ๆ

### กติกา denormalize (จดชื่อซ้ำ)

Firestore ไม่มี JOIN จึงจดชื่อซ้ำไว้ในไฟล์ใบลาโดยตรง เพื่อไม่ต้องเปิด `users`/`leaveTypes` เพิ่มทุกแถว:

| ฟิลด์ที่จดซ้ำ | จดซ้ำมาจาก |
|---|---|
| `leaveRequests.requesterName` | `users.name` |
| `leaveRequests.approverName` | `users.name` |
| `leaveRequests.leaveTypeName` | `leaveTypes.name` |
| `approvals.authorName` | `users.name` |

ราคาที่ต้องจ่าย: ถ้าเปลี่ยนชื่อใน `users` ใบลาเก่าจะยังขึ้นชื่อเดิม — ยอมรับข้อจำกัดนี้ ไม่ต้องเขียนระบบไล่อัปเดต

⚠️ **ตัวพิมพ์เล็ก-ใหญ่ของชื่อฟิลด์ต้องตรงกันทั้งระบบ** — `status` กับ `Status` ถือเป็นคนละฟิลด์ใน Firestore

---

## 3. สถานะใบลา (`status`) — ใช้ได้ 3 ค่านี้เท่านั้น

```
รอพิจารณา  →  อนุมัติ
           └→ ไม่อนุมัติ
```

| สถานะ | ใครเปลี่ยนเป็นค่านี้ได้ | เปลี่ยนต่อไปเป็นอะไรได้ |
|---|---|---|
| `รอพิจารณา` | ระบบตั้งอัตโนมัติตอนสร้างใบใหม่ | `อนุมัติ` หรือ `ไม่อนุมัติ` |
| `อนุมัติ` | ผู้อนุมัติ (`manager`) · ฝ่ายบุคคล (`hr`) | ปลายทาง เปลี่ยนต่อไม่ได้ |
| `ไม่อนุมัติ` | ผู้อนุมัติ (`manager`) · ฝ่ายบุคคล (`hr`) | ปลายทาง เปลี่ยนต่อไม่ได้ |

**กฎการเปลี่ยนสถานะ**

- ใบลาที่สร้างใหม่ทุกใบเริ่มต้นที่ `รอพิจารณา` เสมอ — ตั้งอัตโนมัติ ห้ามให้ผู้ใช้เลือกเอง
- เปลี่ยนได้ตามลูกศรเท่านั้น ห้ามย้อนกลับ
- **ผู้ขอลา (`employee`) เปลี่ยนสถานะไม่ได้**
- ทุกครั้งที่เปลี่ยนสถานะ แก้เฉพาะฟิลด์ `status` เท่านั้น ห้ามเขียนทับฟิลด์อื่นในไฟล์เดิม
- เปลี่ยนเป็น `ไม่อนุมัติ` ต้องมีความเห็นใน subcollection `approvals` อย่างน้อย 1 รายการก่อน
- ลบใบลาได้เฉพาะใบที่สถานะยังเป็น `รอพิจารณา`

---

## 4. ห้ามมี API key หรือรหัสลับใด ๆ ปรากฏในโค้ดหรือไฟล์นี้

- ห้าม hardcode API key, service account key, หรือรหัสลับใด ๆ ลงในไฟล์ที่จะ commit ขึ้น GitHub และห้ามเขียนไว้ใน `CLAUDE.md` เอง
- ค่า config ที่จำเป็น (Firebase config) ให้แยกเก็บในไฟล์ที่ `.gitignore` กันไว้ ไม่ให้ขึ้น GitHub
- ก่อน push ทุกครั้ง ให้ตรวจรายการไฟล์ที่กำลังจะ push ด้วยตาตัวเอง ถ้าเห็นไฟล์ที่มีคำว่า key/secret/config ที่ยังไม่ถูกกัน ห้าม push

⚠️ **สถานะปัจจุบัน (สัปดาห์ที่ 6):** `js/firebase-config.js` ยัง hardcode `apiKey` และค่า Firebase config อื่น ๆ ไว้ตรง ๆ และยังไม่ถูก `.gitignore` กัน — ตาม `leaveeasy-spec.md` หัวข้อ 8 นี่คืองานที่ต้องแก้ใน **สัปดาห์ที่ 7** (เพิ่ม `.gitignore` กันไฟล์คีย์เอง) ยังไม่ต้องแก้ตอนนี้ แต่ห้ามลืมทำตอนถึงสัปดาห์ 7

---

## 5. อ้างอิงเพิ่มเติม

- หน้าจอ 5 หน้า + `index.html`, โครงไฟล์ทั้งหมด, User Story, ข้อมูลตัวอย่าง (seed), แผนการเติบโตรายสัปดาห์ และสิ่งที่ยังไม่ทำใน Module นี้ — ดูรายละเอียดฉบับเต็มใน `leaveeasy-spec.md`
- **ทำเฉพาะสิ่งที่ระบุใน `leaveeasy-spec.md` ห้ามเพิ่มฟีเจอร์ที่ไม่ได้ระบุ และห้ามทำงานของสัปดาห์ถัดไปล่วงหน้า**
